Hybrid choice models
********************

Examples of discrete choice models with latent variables.

The build-only example
:ref:`plot_h08_mode_lv_parameter_overrides` shows how to override a parameter
created automatically by the latent-variable builder.  It follows the
simultaneous Gaussian model, applies a fixed measurement loading, and stops
before estimation so that the documentation focuses on expression
construction.

